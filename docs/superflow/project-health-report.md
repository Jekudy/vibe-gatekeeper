# Project Health Report — vibe-gatekeeper
<!-- updated-by-superflow:2026-04-24 -->

Generated by Superflow Phase 0 onboarding. Auditors: 4 Claude deep-analyst / fast-implementer agents + 1 Codex high-effort reviewer.

## Overview

- **Stack (verified by imports):** Python 3.12 + aiogram 3 (Telegram bot) + FastAPI (admin web) + SQLAlchemy 2.x async + Alembic + asyncpg + redis (prod) + APScheduler + gspread (sync) + pydantic-settings + itsdangerous + Jinja2 + Bootstrap 5 CDN.
- **Size:** 48 Python source files, ~3,819 total LOC across bot/ (34 files, 2,700 LOC), web/ (14 files, 297 LOC), alembic/ (3, 205), tests/ (4, 97), root *.py (5, 520).
- **Tests:** 3 pytest files (test_settings, test_web_app, test_web_auth) + conftest — 9.4% module coverage, ~4% line coverage.
- **Team:** 1 dev, 7 commits in this branch.
- **CI:** GitHub Actions (`.github/workflows/ci.yml` + `release.yml`). GHCR images on main.
- **Deploy:** GitHub → GHCR → Coolify. **Prod cutover completed 2026-04-20** per `docs/runbook.md`.
- **Runtime:** bot + web + postgres:16-alpine + redis:7-alpine via Coolify.
- **Docs on disk:** CLAUDE.md, README.md, SPEC.md (347 lines), docs/runbook.md (135, authoritative), docs/ops/, docs/superpowers/plans+specs.

## Large Files (>500 LOC)

**No files >500 LOC detected** — verified by `find . -name '*.py' -exec wc -l {} +` sorted. Largest file is `bot/services/sheets.py` at 431 LOC. Code quality agent and architecture agent both agreed. The file is healthy by size alone.

## Architecture Violations

| Violation | File:Line | Severity | Details |
|-----------|-----------|----------|---------|
| Web routes bypass `DbSessionMiddleware`; open sessions inline, no commit/rollback | `web/routes/dashboard.py:18`, `web/routes/members.py:16` | HIGH | Safe today (read-only), unsafe as soon as any write is added. No FastAPI DI equivalent to the aiogram middleware exists. |
| Scheduler jobs duplicate session lifecycle: open `async_session()` directly in each job | `bot/services/scheduler.py:30,108,184` | MEDIUM | Three jobs each manage their own session instead of a shared helper. |
| `web` imports from `bot.db` + `bot.config` with no shared `core/` package | `web/config.py:1`, `web/routes/members.py:7-8`, `web/routes/dashboard.py:6-7` | MEDIUM | Renaming anything under `bot/db/` breaks web. No reverse dependency (clean one-way coupling), but implicit. |
| Dual schema path: Alembic for prod, `Base.metadata.create_all` for DEV | `bot/__main__.py:22-29` vs `alembic/versions/001_initial.py` | MEDIUM | Drift risk — dev and prod schemas can diverge silently. |
| Dual vouch source of truth: `Application.vouched_by`/`vouched_at` + `VouchLog` table | `bot/db/models.py:54-55` + `:139-148` | LOW | Two places recording the same fact; reconciliation is manual. |
| SPEC §1 promises 4 tests that don't exist | `SPEC.md:75-78` | LOW | test_questionnaire/vouch/sheets/scheduler — absent. |
| Declared-but-unused deps (install bloat) | `pyproject.toml:11,19,21` (`gspread-asyncio`, `httpx`, `python-dotenv`) | LOW | Zero imports in bot/ or web/. |

## Security Issues (ALL findings — do not truncate)

| Severity | ID | Issue | Location | Evidence / Fix |
|----------|----|-------|----------|----------------|
| **CRITICAL** | SEC-001 | Production-looking `WEB_PASSWORD` value committed into documentation | `docs/runbook.md:110` | Treat as exposed. Rotate immediately in Coolify; scrub value from git history; redact runbook. |
| HIGH | SEC-002 | Weak sensitive defaults: `DATABASE_URL = "postgresql+asyncpg://vibe:changeme@…"`, unauthenticated Redis URL, `WEB_PASSWORD = "admin"` | `bot/config.py:10-11, :19` | Remove defaults for prod-critical settings; fail closed if missing. |
| HIGH | SEC-003 | Session cookie lacks `Secure`; prod served over plain HTTP (`http://187.77.98.73:8080`) | `web/routes/auth.py:37-43` + `docs/runbook.md:74` | Put web behind TLS; set `secure=True`. |
| HIGH | DEV-H1 | Both Dockerfiles run as root | `Dockerfile.bot:1-14`, `Dockerfile.web:1-13` | Add non-root `USER` before CMD. |
| HIGH | DEV-H2 | Alembic migrations baked into bot CMD → bad migration = infinite boot loop | `Dockerfile.bot:14` | Move migrations to a separate step (Coolify pre-deploy hook or init container); add `alembic downgrade` to rollback runbook. |
| HIGH | DEV-H3 | `credentials.json` mounted from legacy host path `/home/claw/vibe-gatekeeper/credentials.json` (7-day retention window has passed) | `docs/runbook.md:104` | Move to `/etc/vibe-gatekeeper/credentials.json` or Coolify secret / GCP Secret Manager. |
| MEDIUM | SEC-004 | No rate-limit / brute-force protection on `/login` | `web/routes/auth.py:21-24` | Add slowapi or Coolify-level rate limit. |
| MEDIUM | SEC-005 | Session signing key derived from `BOT_TOKEN` via sha256 | `web/auth.py:9-10` | Introduce dedicated `WEB_SESSION_SECRET`. |
| MEDIUM | DEV-M1 | Release pushes `:main` (mutable tag); rollback depends on Coolify UI picking a SHA | `.github/workflows/release.yml:53,66` | Pin Coolify to `sha-<hash>` tag. |
| MEDIUM | DEV-M2 | No HEALTHCHECK in Dockerfiles; no redis healthcheck in compose | `Dockerfile.bot`, `Dockerfile.web`, `docker-compose.yml` | Add HEALTHCHECK + redis-cli PING healthcheck. |
| MEDIUM | DEV-M3 | No dependency / image vulnerability scanning (Dependabot, CodeQL, Trivy, pip-audit, bandit, semgrep all absent) | `.github/` | Add `.github/dependabot.yml` for pip; add Trivy scan in ci.yml. |
| MEDIUM | DEV-M4 | No DB backup procedure documented or automated | `docs/runbook.md`, `docs/ops/` | Scheduled `pg_dump` via cron or Coolify lifecycle. |
| MEDIUM | CI-001 | CI runs tests + ruff but no security scanning | `.github/workflows/ci.yml:36-40` | Add pip-audit + Trivy step. |
| LOW | SEC-006 | Unbounded wildcard `ilike` on member name search | `web/routes/members.py:14-29` | Bound input length. |
| LOW | DEV-L1 | Dockerfiles not multi-stage — dev tools ship to prod image | `Dockerfile.bot`, `Dockerfile.web` | Adopt builder → runtime pattern. |
| LOW | DEV-L2 | `.superflow-state.json` missing from `.gitignore` | `.gitignore` | Add entry. |

## Documentation Issues

| Severity | ID | Issue | Location |
|----------|----|-------|----------|
| HIGH | DOC-H1 | `CLAUDE.md` says legacy runtime is still the live path; `docs/runbook.md` records 2026-04-20 cutover complete and legacy stopped | `CLAUDE.md:24` vs `docs/runbook.md:65-75,106-117` |
| HIGH | DOC-H2 | SPEC §7 describes Telegram Login Widget + HMAC-SHA256 web auth; actual is `WEB_PASSWORD` form + itsdangerous cookie | `SPEC.md:283` vs `web/auth.py` + `web/routes/auth.py` |
| HIGH | DOC-H3 | SPEC promises `test_questionnaire/vouch/sheets/scheduler.py` — none exist | `SPEC.md:75-78` |
| MEDIUM | DOC-M1 | README uses `python3.12 -m venv` + `pip install -e .[dev]`; preflight names `uv`; no `[tool.uv]` config in pyproject | `README.md:30-36` |
| MEDIUM | DOC-M2 | SPEC §1 structure diagram outdated (missing `bot/handlers/chat_events.py`, `bot/db/repos/intro.py`) | `SPEC.md:5-79` |
| MEDIUM | DOC-M3 | Top-level scripts (`phone_login.py`, `scan_work.py`, `test_all.py`, `test_flow.py`, `transcribe_voice.py`) undocumented; misnamed `test_*.py` at root not collected by pytest | repo root |
| LOW | DOC-L1 | `/refresh` user command undocumented under SPEC §4 | `bot/handlers/start.py:120` vs `SPEC.md §4.6` |
| LOW | DOC-L2 | Coolify app name still `vibe-gatekeeper-bot-staging` after prod cutover | `docs/runbook.md:88,93` |
| LOW | DOC-L3 | 2026-04-19 coolify-knowledge-layer plan references `~/Vibe/...` paths outside this repo | `docs/superpowers/plans/2026-04-19-coolify-knowledge-layer.md` |
| LOW | DOC-L4 | No static OpenAPI export (live only at `/openapi.json`, `/docs`) | `web/app.py` |
| LOW | DOC-L5 | `staging-cutover.md` historically superseded, no status banner | `docs/ops/vibe-gatekeeper-staging-cutover.md` |

## Code Quality

- **Lint:** `ruff check .` → 0 violations. Configured line-length 100, target py312 (`pyproject.toml:48`).
- **Format:** `ruff format --check .` → **26/55 files would be reformatted**. Formatter never enforced (CI only runs `ruff check`).
- **Pre-commit:** No `.pre-commit-config.yaml`.
- **Type hints:** 92.3% return annotations (84/91 funcs), 95.1% params (156/164). 7 unannotated returns are FastAPI route handlers.
- **TODO/FIXME/HACK/XXX/NOTE:** 0 in Python tree.
- **Duplication / minor smells:**
  - `CQ-L1` — `bot/services/sheets.py:402` executes `select(User)...`, builds dict into `_`, discards it → wasted DB round-trip every 5 min.
  - `CQ-L2` — hardcoded `@vibeshkoder_bot` in kick message ignores `settings.WEB_BOT_USERNAME`. `bot/handlers/chat_events.py:119`.
  - `CQ-L3` — cookie max-age `7*24*60*60` re-inlined at `web/routes/auth.py:40` instead of importing `_COOKIE_MAX_AGE` from `web/auth.py:12`.
  - `CQ-L4` — magic counts `< 5` and `< 8` for refresh-nudge phases in `bot/services/scheduler.py:143,155`.
  - `CQ-L5` — `bot/texts.py:89 CANCELLED` defined, never imported.
  - `CQ-L6` — `web/auth.py:17` uses `password == settings.WEB_PASSWORD` (non-constant-time); prefer `secrets.compare_digest`.
  - `CQ-L7` — mention-formatter `f"@{x.username}" if x.username else x.first_name` duplicated across `bot/handlers/vouch.py:100`, `chat_events.py:112,155`, and similar in `bot/services/sheets.py:343`.
  - `CQ-M1` — formatter not enforced in CI.
  - `CQ-M2` — `bot/config.py:25` and `web/config.py:3` each instantiate their own `Settings()`; pydantic-settings reads env twice, drift risk if one ever caches or mutates.

## DevOps & Infrastructure

- **docker-compose services:** bot, web, db (postgres:16-alpine, has healthcheck), redis (redis:7-alpine, no healthcheck).
- **CI:** `ci.yml` runs pytest + ruff on PR/push to main/master; `release.yml` triggers on CI success on main, builds both images, pushes `sha-<hash>` + `:main` tags to GHCR.
- **Release target:** Coolify watches `:main` (mutable, behaves like `:latest`).
- **Migrations:** baked into bot container CMD (`alembic upgrade head && python -m bot`).
- **Secrets:** Coolify env vars (set via UI); `GITHUB_TOKEN` auto-provided in workflows; `credentials.json` still mounted from legacy host path.
- **Security scanning:** NONE (no Dependabot / CodeQL / Trivy / pip-audit).
- **Backups:** none detected. No `pg_dump` cron, no Coolify backup hook, no restore doc.
- **Env files:** `.env`, `.env.example`, `.env.production.example`, `.env.staging.example` — three examples identical in key set (12 keys, no drift).
- **.gitignore:** covers `.env`, `.venv`, `__pycache__`, `.pytest_cache`, `.ruff_cache`, `.worktrees/`, `dist/`, `build/`; **missing `.superflow-state.json`**.

## Test Coverage

- 3 pytest files (`test_settings.py`, `test_web_app.py`, `test_web_auth.py`) + `conftest.py` = 97 LOC.
- 29 of 32 source modules have zero corresponding test (all repos, all handlers, both services, middleware, keyboards, filters, states, models, dashboard/members routes).
- `test_all.py` (219 LOC) + `test_flow.py` (84 LOC) at repo root: misnamed — they are manual Telethon e2e smoke scripts requiring `TELEGRAM_SESSION` / `TELEGRAM_API_ID`; excluded by `[tool.pytest.ini_options] testpaths = ["tests"]`. They are NOT part of the safety net.

## Entry Points

- **Bot:** `bot/__main__.py` — builds `Bot` + `Dispatcher`, installs `DbSessionMiddleware`, registers 7 routers (start, questionnaire, vouch, admin, chat_events, forward_lookup, chat_messages), MemoryStorage in DEV / RedisStorage in prod, starts `AsyncIOScheduler` in `on_startup`, polls with `allowed_updates=[message, callback_query, chat_member, my_chat_member]`.
- **Web:** `web/app.py` (create_app) + `web/__main__.py` — FastAPI factory, auth middleware redirects unauth to `/login`, mounts `/static`, includes auth/dashboard/members routers; uvicorn on `0.0.0.0:8080`.
- **Migrations:** `alembic/env.py` — uses `Base.metadata`; rewrites `postgresql://` → `postgresql+asyncpg://`.
- **Ops scripts at root (orphan / undocumented):** `phone_login.py`, `scan_work.py`, `transcribe_voice.py` (Telethon user-mode ops), `test_all.py`, `test_flow.py` (manual e2e).

## Technical Debt (Prioritized)

| Priority | Issue | Location | Evidence | Recommendation |
|----------|-------|----------|----------|----------------|
| **P0** | Production WEB_PASSWORD value exposed in committed runbook | `docs/runbook.md:110` | Codex redacted the value; raw text still in git history | Rotate in Coolify immediately; `git filter-repo` or BFG to scrub history; redact runbook |
| **P0** | `credentials.json` mounted from legacy path past 7-day retention | `docs/runbook.md:104` | Runbook flags 7-day rule; cutover was 2026-04-20 | Move to stable host path / Coolify secret before legacy VPS cleanup |
| **P0** | CLAUDE.md misleads readers about current runtime (contradicts runbook) | `CLAUDE.md:24` | Says legacy "remains live", runbook says stopped | Rewrite "Current Migration Rule" section to reflect post-cutover state |
| **P1** | Weak sensitive defaults in config | `bot/config.py:10-11,19` | `changeme`, `admin` | Require envs in prod; no defaults |
| **P1** | Migrations coupled to bot CMD → boot-loop risk | `Dockerfile.bot:14` | `sh -c "alembic upgrade head && python -m bot"` | Extract pre-deploy migration step |
| **P1** | Dockerfiles run as root | `Dockerfile.{bot,web}` | No `USER` directive | Non-root app user |
| **P1** | SPEC.md §7 wildly inconsistent with reality (Telegram Login Widget vs password) | `SPEC.md:283` | Grep returns zero Widget code | Rewrite §7 or implement Widget |
| **P1** | 9% test coverage, no handler / repo / scheduler tests | `tests/` | 3 test files for 32 modules | Plan sprint-based coverage add; prioritise `vouch` + `scheduler` + `sheets` |
| **P1** | Session cookie missing Secure flag on HTTP prod | `web/routes/auth.py:37-43` | Cookie has only `httponly`, `samesite=lax` | Put behind TLS; `secure=True` |
| **P2** | Wasted DB query every 5 min in sheets sync | `bot/services/sheets.py:402` | `_ = {u.id: u for u in ...}` | Delete 2 lines or use result |
| **P2** | Hardcoded `@vibeshkoder_bot` in kick message | `bot/handlers/chat_events.py:119` | Ignores `settings.WEB_BOT_USERNAME` | Format with settings |
| **P2** | Formatter not enforced in CI; 26/55 files unformatted | `.github/workflows/ci.yml:40` | ruff check only | Add `ruff format --check .` |
| **P2** | No dependency scanning | `.github/` | Dependabot / CodeQL / Trivy / pip-audit all absent | Add `.github/dependabot.yml` + Trivy on release |
| **P2** | Rate-limit missing on `/login` | `web/routes/auth.py:21-24` | No slowapi/CSRF middleware | Add slowapi |
| **P2** | Web session secret derived from BOT_TOKEN | `web/auth.py:9-10` | sha256(BOT_TOKEN) | Dedicated `WEB_SESSION_SECRET` |
| **P2** | SPEC §1 + CLAUDE.md sync with real module list (chat_events, intro, /refresh) | `SPEC.md:5-79`, `CLAUDE.md` | Modules exist, docs silent | Refresh diagrams |
| **P2** | Top-level orphan scripts undocumented | repo root | 5 files, zero doc mentions | Move to `ops/` or delete; document purpose |
| P3 | `:main` mutable tag → pinning is Coolify-driven | `release.yml:53,66` | GHCR tag | Pin to sha in Coolify or add semver tags |
| P3 | Redis healthcheck missing | `docker-compose.yml` | 1/4 services have healthcheck | Add redis-cli PING |
| P3 | No automated DB backup | `docs/ops/` | No cron, no Coolify backup config | `pg_dump` cron |
| P3 | Dual schema creation path (Alembic prod / metadata DEV) | `bot/__main__.py:22-29` | DEV bypass | DEV also runs Alembic |
| P3 | Dual vouch truth | `bot/db/models.py:54-55,:139-148` | Application cols + VouchLog | Decide source of truth |
| P3 | Unused deps bloat | `pyproject.toml:11,19,21` | gspread-asyncio/httpx/python-dotenv | Remove |
| P3 | Non-constant-time password compare | `web/auth.py:17` | `password == ...` | `secrets.compare_digest` |
| P3 | Cookie max-age duplicated | `web/auth.py:12` vs `web/routes/auth.py:40` | 7*24*60*60 | Import constant |
| P3 | Staging-suffix Coolify app name post cutover | `docs/runbook.md:88,93` | cosmetic | Rename app |
| P3 | `.superflow-state.json` missing from `.gitignore` | `.gitignore` | new file this run | Add entry |

## Cross-Check Notes

- All agents agree on stack. Codex flagged `uv` as UNVERIFIED (binary present but no `uv.lock`, no doc/CI reference); Documentation agent independently confirmed uv absent from docs. Architecture agent identified `gspread-asyncio`, `httpx`, `python-dotenv` as declared-but-not-imported.
- Documentation agent reported doc-file timestamps of 2026-04-12/04-20 (git log); disk timestamps show 2026-04-24 because this is a worktree checkout. Non-blocking.
- No severity inflation / conflicting claims across the 5 agents.

## Summary of Findings

- **Total:** 45 findings
- **By severity:** 1 critical, 9 high, 15 medium, 20 low
- **Top risks:** password in runbook, weak defaults, doc drift (CLAUDE.md vs runbook, SPEC vs web/auth), credentials.json on legacy path, migration boot-loop risk, Dockerfile root user.
